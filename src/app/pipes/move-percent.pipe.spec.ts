import { MovePercentPipe } from './move-percent.pipe';

describe('MovePercentPipe', () => {
  it('create an instance', () => {
    const pipe = new MovePercentPipe();
    expect(pipe).toBeTruthy();
  });
});
