import { PieceOnSquarePipe } from './piece-on-square.pipe';

describe('PieceOnSquarePipe', () => {
  it('create an instance', () => {
    const pipe = new PieceOnSquarePipe();
    expect(pipe).toBeTruthy();
  });
});
