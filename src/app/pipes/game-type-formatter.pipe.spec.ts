import { GameTypeFormatterPipe } from './game-type-formatter.pipe';

describe('GameTypeFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new GameTypeFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
