import { MoveCountPipe } from './move-count.pipe';

describe('MoveCountPipe', () => {
  it('create an instance', () => {
    const pipe = new MoveCountPipe();
    expect(pipe).toBeTruthy();
  });
});
