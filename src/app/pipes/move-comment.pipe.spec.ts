import { MoveCommentPipe } from './move-comment.pipe';

describe('MoveCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new MoveCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
