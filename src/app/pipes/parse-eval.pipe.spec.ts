import { ParseEvalPipe } from './parse-eval.pipe';

describe('ParseEvalPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseEvalPipe();
    expect(pipe).toBeTruthy();
  });
});
