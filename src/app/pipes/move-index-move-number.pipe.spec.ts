import { MoveIndexMoveNumberPipe } from './move-index-move-number.pipe';

describe('MoveIndexMoveNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new MoveIndexMoveNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
