import { PrintMovesPipe } from './print-moves.pipe';

describe('PrintMovesPipe', () => {
  it('create an instance', () => {
    const pipe = new PrintMovesPipe();
    expect(pipe).toBeTruthy();
  });
});
